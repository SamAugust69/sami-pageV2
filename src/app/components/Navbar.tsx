import { FC } from 'react';

interface NavbarProps {}

const Navbar: FC<NavbarProps> = ({}) => {
	return <nav className=" bg-g-100 border-2 border-t-100 rounded flex min-h-max p-2 w-16"></nav>;
};

export default Navbar;
